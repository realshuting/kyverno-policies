apiVersion: v1
kind: Pod
metadata:
  name: test-coco-pod
  labels:
    coco: enabled
spec:
  containers:
    - name: hello
      image: busybox:latest
      command: ["sh", "-c", "echo Hello from CoCo! && sleep 3600"]
