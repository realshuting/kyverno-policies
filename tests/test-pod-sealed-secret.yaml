apiVersion: v1
kind: Pod
metadata:
  name: test-sealed-secret-pod
  labels:
    coco: enabled
  annotations:
    coco.io/sealed-secret-config: coco-sealed-secret-config
spec:
  containers:
    - name: hello
      image: busybox:latest
      command: ["sh", "-c", "echo Hello from CoCo! && sleep 3600"]
